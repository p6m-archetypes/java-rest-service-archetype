# {{ project_title }}

REST service generated by `java-rest-service-archetype`.

## Build

```bash
mvn verify
```

## Run

```bash
cd {{ project-name }}-server
mvn spring-boot:run
```

Service starts on port `{{ service_port }}`, management on `{{ management_port }}`.
